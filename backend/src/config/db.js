export const db = {
  // Placeholder para la siguiente fase.
  // Aquí viviría la conexión a PostgreSQL usando pg o un ORM.
  query: async () => {
    throw new Error('DB no conectada todavía. Este backend está en fase scaffold.');
  },
};
